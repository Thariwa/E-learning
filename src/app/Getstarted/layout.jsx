import React from 'react'

export const metadata = {
  title: 'Get started - Uidex',
  description: 'Uidex e-learning platform',
}

export default function layout({children}) {
  return (
    <div>{children}</div>
  )
}
